---
"id": "exhaustive-types"
"types":
  "kafka":
    "properties":
      "bootstrap-servers": 
        "type": "string"
  "kafka-ssl":
    "$base": "./kafka"
    # todo: remove support for this.
    # if you want to add properties,
    # create a type.
    # as-is, this states "type = kafka", 
    # but it has properties not from kafka,
    # so doesn't make sense even if it
    # happens to be convenient.
    "properties":
      "something": "string"
  "mssql":
    "properties":
      "user-id": "string"
      "password": "string"
      "host": "string"
    "secrets": [ "user-id", "password" ]    